We are creating an application whose goal is to coordinate the updates and gossip within a community. It will be a progressive web app that uses Cloudflare workers as its backend. It will use OpenRouter for AI access. And it will use local models of your choosing, which can, for example, be Parakeet for transcribing audio into text.

The flow from the perspective of a first time user is they will open the application, which is hosted on grapevine.gratis.sh. And they will create an account. You may have full discretion over login methods, just don't rely on external services. So I don't want to have to send emails or text messages. I'm thinking we should support username and password, but I'm open to using other things that are more modern like passkeys. I don't know enough about them to know whether they rely on third party integrations. I basically don't want to have to create API credentials to do logins.

When creating an account, it is mandatory to supply name, email, WhatsApp, telephone number, and a hub. Hub in this case is a context that is relevant to this community, which is a large group of people spread out over the world, which have chapters in different cities, and the word they use for a place where there's a lot of members is a hub.

Once signed up, an administrator that has appen access over the app will be able to approve pending accounts. So when create an account, you start as pending, and an admin in an overview will be able to move you from pending to accepted or blocked. An admin can move any account into the to the pending or blocked status where pending is basically a pause where when a user opens the application, they see a screen that just says your account is being reviewed. Please come back in a couple of hours. Make sure that when a user is blocked or pending, they cannot submit anything or cause usage of the database or external APIs. The administrator interface is user friendly in the sense that they can see the name, the hub, the telephone number, and the email of every user. And when they click the telephone number, it links to the WA.me slash telephone number. So that's clicking that link will open this user in their WhatsApp application. And the email similarly has a mailto link. They can also keep the member in the appending state, but leave a message so that if the user opens the application, not only do they see account is under review, but they can see any message at the admin left. For example, we don't recognize you, please contact the admin at a certain address or something like that.

I don't know enough about Cloudflare to know how they handle database storage and so forth, but basically I would yet like to use Cloudflare as my primary backend. In the.env file that is in this repository, you will find API keys and account IDs for Cloudflare that you are able to use at your full discretion. There's also an open router API key, which you may use for AI related code.

When a user opens the application, they will be greeted by the latest Grapevine update, which is basically a summary of what has been happening in the community based on the messages that users have sent to the Grapevine. This message is auto-generated every week. This is a configurable variable in the GitHub Actions that deploys this application. At the bottom of the application, there will be a little speaker, or sorry, I mean a microphone icon where when the user clicks it, it starts recording their voice, and a user can basically just ramble an update to the grapevine, which is like sending a message into the grapevine. And that text is then saved into a remote database and associated to their account.

Aside from the periodic grapevine updates, there will be a little search icon, perhaps next to the record icon or at the bottom right of the interface, where when a user taps it, they can ask for updates from the grapevine. There's two things they can do. Either they can select a hub or specific people, or they can ask an arbitrary question. If they select a hub or people and they ask for an update, then the backend will pull in the messages over a certain period of time and synthesize them and summarized them using the open router API to ask an AI to summarize those messages into something condensed. I leave it up to you what model you use on open router, but I do want it to be configurable in the GitHub Action that deploys this application.

When asking an arbitrary question, for example, what is the community talking about right now? All messages over a certain time period are pulled in and are used to through an AI that is called an open router, answer that question. Whenever asking a question about a person, a hub, or in general, there should be a time span selector. Basically a a drop down, something like the last week, the last month, the last quarter, the last year. That will limit the messages through which the backend will search.

From a deployment perspective, I want to make sure that when the code is pushed to this repository, the application is updated through a GitHub Action. There are two kinds of environment variables in this GitHub Action. There are secret ones like API keys for CloudFare workers and open router. And there are configurable ones that are hard-coded into the GitHub Action. For example, the frequency at which the grapevine summarizes the community, or the model that it uses for the summary and whatever other useful things you think are relevant.

It's important that you make sure that the readme file that you create in this directory will tell the programmer that is managing this code how to deploy things, what environment variables they need, how to set up an administrator account, and so forth. You may assume that they are highly technical and sophisticated and are familiar with any concepts that Cloudflare, OpenRouter, and so forth need. They will do further research or ask you questions if something's confusing. Don't be too elaborate. Just make sure that every relevant thing is in there.